import { Typography } from "@mui/material";
import { Box } from "@mui/system";
import React from "react";

const MainScreen = () => {
  return (
    <Box sx={{textAlign: "center"}}>
      <Typography variant="h2">Welcome to the City bike app!</Typography>
    </Box>
  );
};

export default MainScreen;
